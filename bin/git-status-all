#!/usr/bin/env bash
# shellcheck disable=SC2164

for d in $(find ~/Code/github/ -maxdepth 2 -name ".git" 2>/dev/null | sed 's/.git$//g'); do
  (
    cd "$d"
    if [[ ! -z $(git status --porcelain) ]]; then
      printf "\n%s\n" "$d"
      git status -sb
    fi
  )
done
